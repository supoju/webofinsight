# 用户提问

两个都按顺序执行

# Codex 核心结论

- 按用户要求继续执行两个连续步骤：
  - 先统一三页的字体系统与标题层级
  - 再把结果页剩余的局部手写样式进一步压回统一组件组合
- 字体系统调整：
  - 在 `src/app/layout.tsx` 中引入 `next/font/google`
  - 采用 `Noto Sans SC` 作为正文 sans
  - 采用 `Noto Serif SC` 作为 display 字体
  - 通过 CSS variables 注入：
    - `--font-sans-ui`
    - `--font-display-ui`
  - 在 `src/app/globals.css` 中把 `--font-sans` 与 `--font-display` 纳入 token 体系
- 标题层级统一：
  - 新增 `src/components/ui/typography.tsx`
  - 抽象出：
    - `Eyebrow`
    - `DisplayTitle`
    - `HeroScore`
    - `SectionTitle`
    - `Lead`
  - 首页、答题页、结果页的关键标题与导语统一改用这些组件
- 结果页进一步组件化：
  - 在 `src/components/result-client.tsx` 中新增局部复用块：
    - `ResultMetricCard`
    - `ReviewField`
  - 将命中率、平均反应、最容易被骗、人格分析等统计块统一进相同 card 结构
  - 将错题回顾中的“你的答案 / 正确答案”统一进相同字段块
  - 分享区与诊断区继续复用现有 `Card / Badge / Button`
- 验证结果：
  - `npm --prefix game-engine run test -- src/components/ui-verification.test.ts` 通过
  - `npm --prefix game-engine run build` 通过
  - 新增测试覆盖了关键页面标题使用统一 display typography
- 当前工程效果：
  - 首页、答题页、结果页已经形成统一的 sans/display 双字体体系
  - 标题、导语、区块标题的视觉层级一致性明显提升
  - 结果页内部特例样式数量继续减少，更接近统一的设计系统输出

# 元信息

Generated by: Codex CLI  
Date: 2026-03-26  
Project: webofinsight  
Topic: 字体层级统一与结果页进一步组件化
