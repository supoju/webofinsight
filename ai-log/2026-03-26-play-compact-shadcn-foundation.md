# 用户提问

图片太大，不合适。一个屏幕都放不下，另外，关于网页设计，请考虑UI组件库，比如选择像Shadcn/ui这样符合欧美审美、支持暗模式、且易于定制的库，这能一次性大幅提升视觉一致性和交互流畅性

A

# Codex 核心结论

- 用户选择了分层方案 A：
  - 先修正 `play` 页单屏体验
  - 再只引入 `shadcn/ui` 风格的基础组件层
  - 不做整站重写
- 这次工作不是把项目整体切换成完整组件库驱动，而是建立可复用的底层原件，优先服务答题页：
  - `src/lib/utils.ts`
  - `src/components/ui/button.tsx`
  - `src/components/ui/card.tsx`
  - `src/components/ui/badge.tsx`
  - `src/components/ui/progress.tsx`
- 同时补充了接近 `shadcn` 语义的设计 token，在 `src/app/globals.css` 中统一了：
  - `background`
  - `card`
  - `primary`
  - `secondary`
  - `muted`
  - `border`
  - `ring`
- `play` 页调整重点：
  - 主容器从 `max-w-5xl` 收紧到 `max-w-4xl`
  - 看图区单独限制到 `max-w-2xl`
  - 图形外框内边距减小
  - 错觉舞台在大屏下使用更扁的 `16/10` 比例，避免高度过高
  - 题干、标签、引导语、选项统一改为基础 UI 原件承载
- 人机工程学结果：
  - 1366x768 视口下，核心答题流已接近单屏完成
  - 图形仍保持足够辨识度，但不再主导整个视口
  - 用户视线从标题 -> 图形 -> 题干/选项的纵向流更稳定
- 测试与验证：
  - 更新并通过 `src/components/ui-verification.test.ts`
  - 通过 `src/components/gameplay-smoke.test.ts`
  - 全量测试通过：`5` 个测试文件，`13` 个测试通过
  - 生产构建通过：`/`、`/play`、`/result`
- 官方依据：
  - 已核对 `shadcn/ui` 官方文档，确认其支持 Tailwind CSS v4 与 Next.js 的当前接法，适合当前工程技术栈

# 元信息

Generated by: Codex CLI  
Date: 2026-03-26  
Project: webofinsight  
Topic: play 页压缩与 shadcn 风格基础组件接入
