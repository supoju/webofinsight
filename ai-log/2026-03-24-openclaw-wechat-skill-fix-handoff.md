## 1. 用户提问（完整保留）

今天生成微信草稿的时候，你告诉我，顺手修了公众号 skill 的封面生成脚本。阿里云图片接口已经变更，原脚本会因为过期请求头和旧接口地址报 400；  做了最小修正后才完成草稿创建。这个修正在 C:/Users/su_home.codex/skills/wechat-article-publisher/scripts/  generate_cover.py

这个skills我的小龙虾有，也要修改，你输出一个文件或者其他合适的东西，我交个小龙虾openclaw去修改。

## 2. Codex 核心结论

- 用户不要求直接修改另一套环境中的 skill，而是需要一个可转交给 OpenClaw 的交付物。
- 最合适的交付物是：
  - 一份简短问题说明，解释为什么会出现 `400`
  - 一份最小补丁，限定只修改 `generate_cover.py`
- 本次修复的关键点只有四个：
  - 删除过期请求头 `X-DashScope-Async`
  - 接口地址从旧 `text2image/image-synthesis` 切到 `multimodal-generation/generation`
  - 请求体从 `input.prompt` 改成 `input.messages`
  - 响应解析从 `output.results[0].url` 改成 `output.choices[0].message.content[0].image`
- 为了方便移交，已在项目内新增说明文件和 patch 文件，便于 OpenClaw 直接照补丁修改。

## 3. 元信息

Generated by: Codex CLI  
Date: 2026-03-24  
Project: D:\codexproject\poju\webofinsight  
Topic: handoff patch for wechat generate_cover skill fix
