## 1. 用户提问（完整保留）

这个项目是我用worktree方式开发的，请你结合这个项目开发的过程，使用微信公众号的skills，给我写公众号文章，推送到公众号草稿箱，
下面是我的口述内容，主要以我的口述内容为主，从项目的ai-log等找到补充治疗
口述内容 ：前天我研究了一下 Codex多分支多窗口进行代码编写的方法，就是 worktree 的方式。
今天我尝试使用 Codex 的worktree方式进行代码编写。很显然，前天发的文章的技术文档里有很多比较繁琐的命令行，比如生成不同的目录和仓库。但这次我没有严格按照那些细节去做，不过思路还是一样的。
我打开一个主控的 Codex 窗口后，把我关于使用 work tree 方式进行代码编写的需求告诉它，由它来替我出主意和执行。后面的事情就变得简单多了。我按照它的指引给出指令，由它生成了当前的目录结构以及两个分支的目录文档。我还要求它为两个分支文档的最初工作写好提示词。当然，最初关于应用的一些要求和想法是我给到主控codex的。
接着，打开两个分支的窗口运行codex，同时也要求主控窗口替我运行对应的 git 命令和worktree相关命令。这样三个窗口就同时开始运行。
按照我对网页应用设计的想法，我想做一个视觉错觉的游戏网站。这里可以分成内容（或题目）生成模块和与游戏玩法相关的两个部分。这两个部分可以分得比较清晰，也比较方便用这个思路来做分支。互相不会修改对方的代码
然后，两个分支窗口同时存在并运行代码。有一点很清楚：在两个分支窗口同时生成代码时，上下文消耗确实比单个窗口运行要好一些。
问题是，这时需要在两个窗口完成各自的工作后，再将两边的工作情况汇总给主控窗口，告知主控窗口两边发生了什么。

因此，我想到还有一个叫做“Codex App”的工具（它已有Windows版本）。据说，如果使用这个App，它可以在主控窗口监控各个分支窗口的状况。但目前，主控窗口还看不到其他窗口的活动，只能等待各自将情况汇总回来。不过，主控窗口可以读取整个目录的文件变化，所以对两边的变化它还是能够进行检查和审计的。这或许会带来双重token消耗。
当然，这里也出现了一点问题。一个不好的地方是，你必须清楚知道两个窗口各自的工作是什么。窗口没有明确清晰的名字，都是cmd终端。如果一旦对窗口的判断出错，将各自的命令或提示词给错了地方，就可能会造成比较糟糕的问题。第一次我就将提示词复制错了地方，导致工作暂停。好在没有造成不良后果，因为窗口当前在哪个目录，你只能通过Codex窗口最底部的目录显示才能知道，这也是一个不方便工作的地方。
最终，我还是完成了这项工作。另一点是，当两边的工作进度不一致时，也只能等到两个窗口对应的工作都完成后，才能进行下一步。
回到主控窗口。当然，前面说到的主控窗口去合并各个分支等工作，都可以用自然语言和主控窗口进行对话来完成。这些事情做完之后，后续合并提交完成，主要的工作就回到主控窗口，去做一些完善以及本地部署和显示。
应该说，这是一次比较简单、初级的，使用 worktree 方式进行代码开发的状况。后续我还是要下载一下 Codex 的 App，看看它是否会对这种多窗口开发有帮助。因为前段时间我在做另一个项目开发的时候，对话窗口长期是不够用的。那些能够开发更多代码的大项目，应该是一定要用到多窗口的协同配合。否则，上下文一旦过多，造成 Codex “失忆”的话，那么代码的效果就难以得到保证了。

## 2. Codex 核心结论

- 文章应以用户第一人称实战观察为主，不写成泛泛的 Git 教程，也不把重点放在命令行细节上。
- 项目内可用于补充的核心素材包括：
  - `docs/worktrees.md` 中明确的三方职责：`main/orchestrator`、`content-lab`、`game-engine`
  - `ai-log/2026-03-23-optical-illusion-game-planning.md` 中的主控规划与 worktree 分工
  - `ai-log/2026-03-23-content-lab-content-assets.md` 与 `ai-log/2026-03-23-game-engine-implementation.md` 中的双分支实际产出
- 文章的中心判断应落在“多窗口协作的真正成本不在命令，而在主控调度、边界清晰、窗口辨识、进度回报与合并收口”。
- 文章中应保留并强化以下真实体验：
  - 主控窗口先搭结构、定边界、写分支初始提示词
  - 两个分支窗口并行运行后，上下文压力明显小于单窗口
  - 主控窗口当前无法直接看到分支窗口活动，只能等待回报或读取目录变化审计
  - 发错提示词到错误窗口会立刻造成协作中断风险
  - 当两个分支进度不一致时，后续步骤仍受制于汇总与同步
  - 最终合并、完善、本地部署与显示，仍回到主控窗口完成
- 发布策略应遵循 `wechat-article-publisher` skill 的默认规则：只推送到草稿箱，不直接发布。

## 3. 元信息

Generated by: Codex CLI  
Date: 2026-03-24  
Project: D:\codexproject\poju\webofinsight  
Topic: codex worktree multiwindow wechat article
