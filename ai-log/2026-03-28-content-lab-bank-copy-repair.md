### 1️⃣ 用户提问（完整保留）

“继续上次任务,上次补充了content-lab的题目，

### 2️⃣ Codex 核心结论

- 继续上次 `content-lab` 题库扩容任务时，发现 `content-lab/data/questions.json` 的扩容结构还在，但几乎所有中文内容字段都已退化成 `?` 占位，不能直接交付。
- 已先核对现有契约边界，确认本次只修复内容资产，不改动这些结构约束：
  - 顶层数组结构
  - `84` 条总题量
  - `80` 条 `score` + `4` 条 `analysis`
  - 现有 `id`、`score`、`difficulty`、`generator` / `asset`、`renderVariant`
- 已重写 `content-lab/data/questions.json` 的题面文案，使其恢复为可交付中文内容，覆盖：
  - `title`
  - `prompt`
  - `options`
  - `answer`
  - `explanation`
  - `uiCopy`
  - `psychologyGoal`
- 文案策略：
  - 保留扩容后的 mixed bank 结构
  - 按 renderer family 与 `renderVariant` 补足题目差异化描述
  - 对 `duck-rabbit` 与 `old-young-woman` 维持“识别 + 切换 + 解释”三层题法
  - 保持结果页可消费的心理目标与分析题信号
- 内容层校验结果：
  - `questions.json` 总数为 `84`
  - 模式分布为 `80` 条 `score`、`4` 条 `analysis`
  - 未再发现 `???` 式占位残留
- 工程验证情况：
  - `npm --prefix game-engine run test` 未能完成
  - `npm --prefix game-engine run build` 未能完成
  - 失败主因不是题库 schema 报错，而是当前本地 Node / Vitest / Next.js 进程出现内存不足：
    - `JavaScript heap out of memory`
    - `Zone Allocation failed - process out of memory`
- 风险说明：
  - 当前内容资产本身已恢复，但项目级自动化验证仍需在更稳定的本机环境或更高内存限制下重跑。
  - 本次未触碰 `game-engine` 逻辑，若后续出现渲染或 schema 问题，更可能来自既有工程环境，而不是本次文案修复本身。

### 3️⃣ 元信息

Generated by: Codex CLI  
Date: 2026-03-28  
Project: webofinsight  
Topic: content-lab question bank copy repair
