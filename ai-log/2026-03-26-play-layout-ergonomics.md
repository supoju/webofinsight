# 用户提问

请拉起这个网页项目，用默认端口，我查看网页来指导你修改完善。

[Image #1] 这样的题目和选择题设置，眼睛要转动才能从选项到图片，不符合人性或者人机工程学。应该改为更合适的布置。请认真思考这个问题再进行部署。

# Codex 核心结论

- 问题性质不是单纯视觉样式问题，而是答题页的信息架构问题。
- 原布局采用桌面双栏：左侧题干与选项，右侧错觉图。用户在观察刺激图形和提交判断之间，需要频繁进行大幅水平扫视，不符合“看图后立即判断”的任务路径。
- 结合该项目题型特征，`content-lab/data/questions.json` 中当前 20 道题的选项数均为 3 个，适合改为图下直接作答的单主路径布局，而不必保留左右并置的操作区。
- 这次调整采取最小改动策略：
  - 不改题库、计时、判分、跳题和结果流。
  - 仅重构 `game-engine/src/components/game-client.tsx` 的信息编排。
  - 新布局改为：顶部状态条 -> 中央看图区 -> 图下作答区 -> 底部进度说明。
- 为防止回退到旧结构，新增了可验证语义分区：
  - `region "Illusion stage"`
  - `region "Answer controls"`
- 测试先行验证：
  - 在 `game-engine/src/components/ui-verification.test.ts` 增加失败测试，约束“看图区必须先于作答区出现，且作答区包含题干与选项”。
  - 完成布局改造后，相关测试转绿。
- 验证结果：
  - `npm --prefix game-engine run test -- src/components/ui-verification.test.ts` 通过
  - `npm --prefix game-engine run test -- src/components/gameplay-smoke.test.ts` 通过
  - `npm --prefix game-engine run test` 全量通过，结果为 `5` 个测试文件、`13` 个测试通过
- 浏览器快照确认：
  - 页面主顺序已变为标题状态 -> `Illusion stage` -> `Answer controls`
  - 解决了原先“图和选项横向分离过远”的问题

# 元信息

Generated by: Codex CLI  
Date: 2026-03-26  
Project: webofinsight  
Topic: play 页面人机工程学布局调整
