### 1️⃣ 用户提问（完整保留）

这个项目两次由你拉起，我预计本地3000端口查看，都系统奔溃了。现在你只做一半，提供127.0.0.1,3000端口就好了，我来打开本地网页

### 2️⃣ Codex 核心结论

- 问题分析：
  之前的开发服务器使用 `next dev` 默认模式启动，`Next.js 16.2.1` 在该环境下默认启用 `Turbopack`。
  现有日志 `devserver-20260329.err.log` 显示 Node/V8 发生致命内存错误：
  `Fatal process out of memory: Failed to reserve virtual memory for CodeRange`
  以及 `Fatal process out of memory: Re-embedded builtins: set permissions`。
- 关键决策：
  不修改业务代码，不继续实施额外任务，仅改用更稳定的开发启动方式：
  `next dev --webpack --hostname 127.0.0.1 --port 3000`
- 结果：
  本地服务已在 `127.0.0.1:3000` 监听，并通过 `curl.exe -I http://127.0.0.1:3000` 返回 `HTTP/1.1 200 OK`。
- 风险说明：
  当前只是稳定拉起本地开发服务，没有修复 Turbopack 在该机器上的根因。
  若后续再次使用默认 `npm run dev`，仍可能触发同类 OOM 崩溃。
- 最终结论：
  当前可用访问地址为 `http://127.0.0.1:3000`，适合用户本地直接打开查看页面。

### 3️⃣ 元信息

Generated by: Codex CLI  
Date: 2026-03-29  
Project: webofinsight  
Topic: 本地 3000 开发服务改用 webpack 稳定拉起  
